# this md describe the service for semi standard apps  
    1. you should read the standard text from standard.pdf file. find the relate standard
    2. create rust (2024 edition) project for this apps. each app should have a lib create for common trait and struct define, and a bin for specific app entry.
    3. may be a common state machine should be designed for this.
    4. each bin app can be deploy as a system service (windows or linux)
    5. each bin app should run as grpc service
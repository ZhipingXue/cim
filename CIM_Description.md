# here i want to create a semiconductor equipment cim app group. 
## the app group contains semi standard function job manager app( cjob E94, pjob E40 standard), carrier manager app(E87). substrate tracing (E90), and alarm,event,and state machine should match E116 standard. besides, equipment metadata manager(E125) should also served as a app;

## the app group should contains a cim_host process and system service manager.(this app has designed)

## the app group should contains multi control app for each chamber, robot, substrate cache, loadport module. 


## a data collect service should be designed also.
# detail describe is in other md file
# well defined coding struct should be used
